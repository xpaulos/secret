## Please paste the result below this line

---

The result I got it was the expected one. The name I provided with the random number after it. I could not find something to change and generate a unique result.
So I made the effort to make it more immersive for the user by adding the input function so they can provide whatever name they want and added on the top of the file the shebang "#!/usr/bin/env" so if they want to make it executable directly. Also, I changed the extension of the inital file to ".py" as it was clear for me that it is a python script. I hope this makes up for something.
